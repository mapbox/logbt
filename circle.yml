machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - cat /proc/sys/kernel/core_pattern
    - docker build --rm=false -t logbt-springmeyer -f Dockerfile.springmeyer-logbt-readonly .

test:
  override:
    - docker run logbt-springmeyer
    - cat /proc/sys/kernel/core_pattern
