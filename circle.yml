machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - cat /proc/sys/kernel/core_pattern
    - docker build --rm=false -t logbt-precise -f Dockerfile.ubuntu-precise .

test:
  override:
    - docker run --privileged logbt-precise
    - cat /proc/sys/kernel/core_pattern
